- scope: 'component'
  name: 'el-dialog'
  type: 'props'
  version: '2.3.0'
  commit_hash: '412c79c'
  description: |
    We don't need `custom-class`, but use the `class` directly.
  props:
    - api: 'custom-class'
      before: string
      after: '-'
