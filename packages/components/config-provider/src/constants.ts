import type { ConfigProviderProps } from './config-provider-props'

export type ConfigProviderContext = Partial<ConfigProviderProps>

export const configProviderContextKey = 'CONFIG_PROVIDER_CONTEXT_KEY'
